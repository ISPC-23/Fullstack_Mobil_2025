<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.TiendaFull" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/c_primary</item>
        <item name="android:logo">@drawable/logo_tienda_login</item>
        <item name="android:fontFamily">@font/geist_regular</item> <!-- Agrega Font Family -->
        <item name="fontFamily">@font/geist_regular</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorControlNormal">@color/black</item>
    </style>

    <style name="Theme.TiendaFull" parent="Base.Theme.TiendaFull">
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="ThemeOverlay.TiendaFull.Toolbar" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="colorControlNormal">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>

</resources>
